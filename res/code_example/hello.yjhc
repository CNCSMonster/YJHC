#include <stdio.h>
#include <stdlib.h>

//
typedef struct struct_people{
    char name[100];
    int age;
    void sayHello(char* helloTo);
}People;
People getPeople(char* name){
    //strcpy(name,name);
    strcpy(self.name,name);
}
People->void sayHello(char* helloTo){
    printf("Hello,%s!",helloTo);
}
int main(){
    int a,b,c;
    int a=1,b=2,c=3;
    People Mike=getPeople();
    People* mkp=&Mike;
    mkp->age=1;
    Mike.sayHello();
    return 0;
}