#include <stdio.h>
#include <stdlib.h>

struct man{
    int age;
    void sayHi();
}

struct man->void sayHi(){
    printf("hi,I'm %d years old",age);
}

int main(){
    struct man m={
        .age=11
    };
    m.sayHi();
    return 1;
}